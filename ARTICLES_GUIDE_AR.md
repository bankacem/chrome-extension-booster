# دليل إضافة المقالات (ExtensionTo)

يشرح هذا الدليل كيفية إضافة المقالات إلى المشروع بشكل صحيح لضمان ظهورها في خريطة الموقع (Sitemap) ومحركات البحث دون مشاكل.

## 1. بنية المشروع
يعتمد المشروع على **نظام محتوى هجين**:
1. **Supabase**: المصدر الرئيسي لتحرير وإدارة المقالات.
2. **ملفات Markdown**: نسخ محلية في `public/content/articles/` لضمان السرعة وتحسين الـ SEO.
3. **الفهرسة (Indexing)**: ملف مركزي `articles-index.json` تستخدمه الواجهة الأمامية.
4. **خريطة الموقع (Sitemap)**: يتم توليدها تلقائياً من الفهرس.

---

## 2. كيف تضيف مقالاً جديداً

### الطريقة أ: عبر Supabase (المنصح بها)
1. اذهب إلى لوحة تحكم مشروعك في Supabase.
2. أضف صفاً جديداً في جدول `articles`.
3. **الحقول الضرورية**:
   - `status`: يجب أن تكون `published` (بالأحرف الصغيرة).
   - `slug`: استخدم رابطاً نظيفاً (مثال: `best-chrome-extensions-2026`).
   - `title`, `content`, `excerpt`: املأ هذه الحقول.
   - `meta_description`, `tags`, `keywords`: هامة جداً للـ SEO.
4. قم بتشغيل أمر المزامنة محلياً:
   ```bash
   bun run publish
   ```
   هذا الأمر سيقوم بـ:
   - جلب المقال من Supabase.
   - إنشاء ملف Markdown المحلي.
   - إعادة بناء الفهرس وخريطة الموقع.
   - رفع التغييرات إلى GitHub.

### الطريقة ب: عبر ملفات Markdown المحلية (للمتقدمين)
1. أنشئ ملفاً جديداً في `public/content/articles/`.
2. اتبع هيكلية المجلدات المقسمة (مثال: `public/content/articles/b/e/s/best-slug.md`).
3. تأكد من صحة الـ Frontmatter (القسم بين `---`):
   ```yaml
   ---
   id: unique-uuid-or-slug
   title: عنوان المقال هنا
   slug: best-slug
   status: published
   published_at: '2026-06-11'
   ---
   ```
4. شغّل `bun run sync-articles` لتحديث الفهرس.

---

## 3. ضمان الظهور في خريطة الموقع (Sitemap)
يتم توليد الـ Sitemap بناءً على ملف `public/content/articles-index.json`.
إذا كان مقالك موجوداً في هذا الملف، فسيظهر **حتماً** في الـ Sitemap.

للتحقق يدوياً:
1. تأكد من وجود الملف: `public/content/articles/x/y/z/your-slug.md`.
2. تأكد من وجوده في الفهرس: افتح `public/content/articles-index.json` وابحث عن الـ slug الخاص بك.
3. شغّل المولد: `bun run sitemap`.
4. تحقق: افتح `public/sitemap.xml`.

---

## 4. تجنب المشاكل الشائعة
- **الروابط المكررة (Duplicate Slugs)**: يجب أن يكون لكل مقال رابط (slug) فريد.
- **المعرفات المكررة (Duplicate IDs)**: يجب أن يكون لكل مقال ID فريد. إذا قمت بنسخ ملف Markdown، تذكر تغيير الـ `id` في الأعلى.
- **عناوين H1**: لا تستخدم وسوم `<h1>` داخل محتوى المقال. النظام يضيف العنوان تلقائياً.
- **الحالة (Status)**: المقالات ذات الحالة `published` فقط هي التي ستظهر في الموقع والـ sitemap.
- **الزيادات في الروابط**: تجنب الحروف العشوائية في نهاية الرابط (مثل `-mo4p804c88h`) إلا للضرورة القصوى.

---

## 5. ملخص الأوامر
| الأمر | الوصف |
| :--- | :--- |
| `bun run publish` | مزامنة كاملة + بناء الفهرس + بناء السيت ماب + الرفع لـ Git |
| `bun run sync-db-to-md` | جلب المقالات الجديدة من Supabase |
| `bun run sync-articles` | إعادة بناء فهرس JSON من الملفات المحلية |
| `bun run sitemap` | إعادة بناء ملف `sitemap.xml` |
| `bun run index-sitemap` | إرسال الروابط الجديدة لجوجل |

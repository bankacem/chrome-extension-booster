# دليل مشروع ExtensionTo (التوثيق التقني)

هذا المستند يشرح بالتفصيل بنية المشروع، كيفية إدارة المقالات، نظام المزامنة، وتوليد الخرائط البرمجية (Sitemap).

---

## 1. بنية المشروع (Architecture)

المشروع مبني باستخدام التقنيات التالية:
- **Frontend**: Vite + React + TypeScript + Tailwind CSS.
- **Backend/Database**: Supabase (قاعدة البيانات وإدارة المحتوى).
- **Hosting**: Vercel.
- **Content Strategy**: نظام هجين يجمع بين قاعدة البيانات وملفات Markdown الثابتة (Static Files) لضمان سرعة الأداء وتحسين محركات البحث (SEO).

---

## 2. إدارة المقالات (Article Management)

يتم تخزين المقالات في مسارين:
1. **قاعدة البيانات (Supabase)**: هي المصدر الرئيسي (Source of Truth) حيث يتم إنشاء وتحرير المقالات.
2. **ملفات Markdown**: يتم تحويل المقالات من قاعدة البيانات إلى ملفات `.md` محلية داخل المشروع لتمكين Vercel من فهرستها وتقديمها بسرعة فائقة.

### مسار تخزين الملفات:
تخزن المقالات في `public/content/articles/` باستخدام نظام **Partitioning** (تقسيم المجلدات بناءً على أول 3 أحرف من الرابط - slug) لتجنب وجود آلاف الملفات في مجلد واحد، مما يحسن أداء نظام الملفات.
مثال: رابط `best-extension` سيخزن في `public/content/articles/b/e/s/best-extension.md`.

---

## 3. دورة حياة النشر والمزامنة (Sync Pipeline)

تتم عملية المزامنة عبر عدة مراحل برمجية:

### أ. المزامنة من قاعدة البيانات إلى Markdown (`sync-db-to-md.ts`)
يقوم هذا السكربت بجلب المقالات ذات الحالة "Published" من Supabase وتحويلها إلى ملفات Markdown مع Frontmatter يحتوي على الميتا-داتا (العنوان، الوصف، الكلمات المفتاحية).

### ب. بناء الفهرس (`sync-articles.ts`)
يقوم بمسح مجلد المقالات وإنشاء ملف JSON موحد في `public/content/articles-index.json`. هذا الملف تستخدمه الواجهة الأمامية لعرض قائمة المقالات والبحث فيها دون الحاجة لطلب قاعدة البيانات في كل مرة.

---

## 4. توليد خريطة الموقع (Sitemap Generation)

يتم توليد خريطة الموقع تلقائياً عبر سكربت `generate-sitemap.ts`.

### كيف يعمل؟
1. يجمع الصفحات الثابتة (الرئيسية، المدونة، الخصوصية).
2. يقرأ جميع المقالات من `articles-index.json` ويضيف روابطها.
3. يضيف روابط الإضافات (Extensions) من بيانات المشروع.
4. يطبق قواعد **Pillars** (المقالات الأساسية) ليعطيها أولوية أعلى (0.9) وتكرار فحص أسبوعي.
5. ينتج الملف النهائي في `public/sitemap.xml`.

---

## 5. أوامر التشغيل (Execution Commands)

يمكنك تشغيل كل مرحلة يدوياً أو استخدام أمر النشر الشامل:

| المهمة | الأمر | الوصف |
| :--- | :--- | :--- |
| **المزامنة الشاملة** | `bun run publish` | يقوم بالمزامنة، بناء الفهرس، الرفع لـ GitHub، والأرشفة في Google. |
| **مزامنة البيانات** | `bun run sync-db-to-md` | يجلب المقالات الجديدة من Supabase. |
| **تحديث الفهرس** | `bun run sync-articles` | يحدّث ملف JSON الخاص بالمقالات. |
| **توليد الـ Sitemap** | `bun run sitemap` | ينشئ ملف `sitemap.xml` الجديد. |
| **الأرشفة اليدوية** | `bun run index-sitemap` | يرسل روابط الموقع إلى Google Indexing API. |

---

## 6. تحسين محركات البحث (SEO & Indexing)

يحتوي المشروع على نظام متكامل للأرشفة الفورية:
- **Google Indexing API**: بمجرد نشر مقال جديد، يتم إرسال طلب أرشفة فورية لجوجل عبر سكربت `google-indexing.ts`.
- **Automatic Internal Linking**: يتم ربط المقالات ببعضها تلقائياً بناءً على الكلمات المفتاحية والوسوم لتعزيز الـ Crawling.
- **Clean Slugs**: يتم تنظيف الروابط من الرموز العشوائية وضمان هيكلية SEO قوية.

---

## 7. ملفات هامة في المشروع
- `vercel.json`: إعدادات التوجيه (Redirects) وRewrites لضمان عمل الـ SPA والـ Sitemap بشكل صحيح.
- `src/pages/BlogPost.tsx`: المكون المسؤول عن عرض المقال وفك تشفير ملفات Markdown.
- `scripts/indexed-urls.json`: سجل يتبع الروابط التي تم إرسالها بنجاح لجوجل لتجنب تكرار الطلبات وتجاوز الحصص اليومية.

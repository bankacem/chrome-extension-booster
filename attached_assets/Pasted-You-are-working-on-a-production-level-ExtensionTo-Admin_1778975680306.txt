You are working on a production-level ExtensionTo Admin Dashboard for a blog platform (React + Vite + Supabase).

CURRENT PROBLEM:
The Admin Dashboard UI is currently static or partially mocked:
- Article counts are not real-time
- Recent articles are hardcoded or fake
- CMS actions do not actually write to database
- SEO Dashboard is not connected to real article data
- "AI Generator" and "CMS Creator" are not fully functional

────────────────────────────
GOAL:
────────────────────────────

Transform the Admin Dashboard into a FULLY FUNCTIONAL REAL SYSTEM powered by Supabase.

All data must come from REAL database queries — no mock or static data.

────────────────────────────
1. REAL ARTICLES DATA (CRITICAL)
────────────────────────────

Connect dashboard to Supabase table: "articles"

Fetch:
- total number of articles (count)
- latest 5–10 articles
- article title
- slug
- category
- created_at

Replace ALL hardcoded values with real Supabase queries.

────────────────────────────
2. LIVE METRICS
────────────────────────────

Replace static values:

❌ "484 articles published"
✔ must be: real COUNT(*) from Supabase

❌ fake stats cards
✔ must be dynamic and auto-updating

────────────────────────────
3. RECENT ARTICLES SECTION
────────────────────────────

Must be dynamically loaded:

ORDER BY created_at DESC
LIMIT 5 or 10

Each item must include:
- title
- slug link (/blog/{slug})
- category
- date

No fake or hardcoded articles allowed.

────────────────────────────
4. CMS CREATOR (REAL WRITE SYSTEM)
────────────────────────────

When user creates article:
- Save to Supabase "articles" table
- Include:
  title
  slug
  content (markdown or html)
  category
  created_at
  author = "Daniel Carter"

After publish:
- return live URL
- update dashboard immediately

────────────────────────────
5. SEO DASHBOARD (REAL DATA)
────────────────────────────

For each article:
- check title length
- check meta description exists
- check slug format
- check missing fields

All checks must run from real DB data.

────────────────────────────
6. AI GENERATOR (OPTIONAL BUT REAL)
────────────────────────────

If enabled:
- generate article draft
- save as "draft" in Supabase
- do NOT publish automatically

────────────────────────────
7. PERFORMANCE RULES

- Use efficient Supabase queries
- Avoid multiple redundant fetch calls
- Use caching where needed
- Do NOT block UI on fetch failure

────────────────────────────
8. ERROR HANDLING (IMPORTANT)

If Supabase fails:
- show fallback UI message
- DO NOT break dashboard
- DO NOT logout user

────────────────────────────
9. SECURITY RULES

- Keep /admin routes protected
- Do not expose service keys
- Only use publishable/anon key
- Do not modify public blog rendering system

────────────────────────────
FINAL GOAL:

Turn the Admin Dashboard into a REAL CMS CONTROL CENTER:

✔ live articles from Supabase
✔ real counts and analytics
✔ real CMS publishing system
✔ no mock data anywhere
✔ production-ready admin panel